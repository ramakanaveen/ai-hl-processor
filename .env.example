# Google Cloud Configuration (Required for UAT/Production)
GOOGLE_CLOUD_PROJECT=your-google-cloud-project-id
GOOGLE_CREDENTIALS_PATH=/path/to/your/service-account-key.json

# Environment Selection (Optional - defaults to 'test')
# Options: test, dev, uat, prod
ENV=test