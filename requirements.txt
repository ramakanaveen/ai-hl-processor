# AI Headline Impact Processor - Core Dependencies
# Minimal required packages for the system to function

# Core Data Processing
numpy>=1.26.0
pandas>=2.0.0

# Google Cloud / Vertex AI (for Gemini Flash)
google-cloud-aiplatform>=1.38.0

# Configuration and Environment
python-dotenv>=1.0.0

# FastAPI Backend (Phase 1)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
websocket-client>=1.9.0  # Client-side WebSocket
websockets>=16.0  # Server-side WebSocket support
wsproto>=1.3.0  # WebSocket protocol implementation

# Streamlit UI (Phase 1)
streamlit>=1.28.0
plotly>=5.17.0
requests>=2.31.0

# LLM Providers (Phase 1.5)
anthropic>=0.25.0  # For Claude support (optional)

# News Feed Integration
feedparser>=6.0.0  # For RSS feed monitoring

# Optional: Development and Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0